# 複製此檔案為 .env 並填入您的設定
# ===================================

# --- 核心設定 ---
# Google Gemini API Key (必要)
GEMINI_API_KEY="YOUR_GEMINI_API_KEY_HERE"

# --- 路徑設定 (可選，若不設定則使用預設值) ---
# 要掃描的日誌檔案所在的目錄
# LMS_TARGET_LOG_DIR="/var/log/LMS_LOG"
# 結構化分析結果的匯出路徑
# LMS_ANALYSIS_OUTPUT_FILE="/var/log/analyzer_results.json"
# 此腳本自身運維日誌的儲存路徑
# LMS_OPERATIONAL_LOG_FILE="data/analyzer_script.log"
# 專案資料根目錄 (狀態檔、向量索引等將存於此)
# LMS_HOME="."

# --- 模型與演算法設定 (可選) ---
# Sentence Transformer 的嵌入模型名稱
# EMBEDDING_MODEL_NAME="paraphrase-multilingual-MiniLM-L12-v2"
# LLM 模型名稱
# LMS_LLM_MODEL_NAME="gemini-1.5-flash-latest"

# --- 效能與成本控制 (可選) ---
# LRU 快取大小
# LMS_CACHE_SIZE=10000
# 啟發式評分後，取分數最高的日誌百分比進行深度分析
# LMS_SAMPLE_TOP_PERCENT=20
# LLM 批次分析的日誌數量
# LMS_LLM_BATCH_SIZE=10
# 每小時 LLM 費用上限 (美元)
# LMS_MAX_HOURLY_COST_USD=5.0

# --- FAISS 相似度閾值 (可選) ---
# SIM_T_ATTACK_L2_THRESHOLD=0.3
# SIM_N_NORMAL_L2_THRESHOLD=0.2